/** @ingroup Gameplay */
/** @{ */
/** @file level.h */
/** @} */

#ifndef LEVEL_H
#define LEVEL_H

#define L_EMPTY	 0 /**< The tile is empty (traversed normally). */
#define L_WALL	 1 /**< The tile is a wall tile (can't be traversed). */
#define L_STAIR	 2 /**< The tile is a stair tile (sends the entity to a different floor). */

#endif
